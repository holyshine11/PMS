---
phase: 02-easy-payment
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "./gradlew compileJava가 BUILD SUCCESSFUL로 완료된다"
    - "간편결제 전체 플로우가 코드 수준에서 완결성을 갖춘다"
  artifacts: []
  key_links: []
---

<objective>
Final build verification and end-to-end code review for the easy payment (billkey) feature.

Purpose: Confirm that all gap-filling changes from Plans 01 and 02 integrate cleanly. Verify compilation, unit tests, and review the complete feature flow for any remaining issues.

Output: Verified build, test results, and human sign-off on the feature completeness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-easy-payment/02-CONTEXT.md
@.planning/phases/02-easy-payment/02-01-SUMMARY.md
@.planning/phases/02-easy-payment/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 빌드 및 단위 테스트 실행</name>
  <files></files>
  <read_first>
    hola-pms/hola-reservation/src/main/java/com/hola/reservation/booking/gateway/MockPaymentGateway.java,
    hola-pms/hola-reservation/src/main/java/com/hola/reservation/booking/controller/EasyPayApiController.java,
    hola-pms/hola-reservation/src/main/java/com/hola/reservation/booking/service/EasyPayCardServiceImpl.java,
    hola-pms/hola-app/src/main/resources/static/js/booking.js
  </read_first>
  <action>
Run the following verification commands in sequence:

1. **Full Java compilation:**
   ```bash
   cd /Users/Dev/PMS/hola-pms && ./gradlew compileJava
   ```
   Expected: BUILD SUCCESSFUL

2. **Reservation module unit tests:**
   ```bash
   cd /Users/Dev/PMS/hola-pms && ./gradlew :hola-reservation:test
   ```
   Expected: BUILD SUCCESSFUL, including EasyPayCardServiceImplTest

3. **Check that all EasyPay Java files compile without warnings:**
   ```bash
   cd /Users/Dev/PMS/hola-pms && ./gradlew compileJava --warning-mode all 2>&1 | grep -i "easypay\|easy_pay\|warning" | head -20
   ```

4. **Verify booking.js has no obvious syntax issues** by checking the file can be parsed:
   ```bash
   node -e "try { require('fs').readFileSync('/Users/Dev/PMS/hola-pms/hola-app/src/main/resources/static/js/booking.js', 'utf8'); console.log('JS file readable, ' + require('fs').statSync('/Users/Dev/PMS/hola-pms/hola-app/src/main/resources/static/js/booking.js').size + ' bytes'); } catch(e) { console.error(e); process.exit(1); }"
   ```

5. **Verify key code patterns are in place:**
   ```bash
   grep -n "registerTransaction\|approveAfterAuth" /Users/Dev/PMS/hola-pms/hola-reservation/src/main/java/com/hola/reservation/booking/gateway/MockPaymentGateway.java
   grep -n "required = false" /Users/Dev/PMS/hola-pms/hola-reservation/src/main/java/com/hola/reservation/booking/controller/EasyPayApiController.java
   grep -n "이메일을 먼저 입력" /Users/Dev/PMS/hola-pms/hola-app/src/main/resources/static/js/booking.js
   grep -n "pageshow" /Users/Dev/PMS/hola-pms/hola-app/src/main/resources/static/js/booking.js
   ```

If any step fails, diagnose and fix. Document all results.
  </action>
  <verify>
    <automated>cd /Users/Dev/PMS/hola-pms && ./gradlew compileJava && ./gradlew :hola-reservation:test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileJava` output contains "BUILD SUCCESSFUL"
    - `./gradlew :hola-reservation:test` output contains "BUILD SUCCESSFUL"
    - MockPaymentGateway.java contains both "registerTransaction" and "approveAfterAuth"
    - EasyPayApiController.java contains "required = false" (at least 2 occurrences)
    - booking.js contains "이메일을 먼저 입력"
    - booking.js contains "pageshow"
  </acceptance_criteria>
  <done>Full compilation and reservation module unit tests pass. All gap-filling changes verified in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 간편결제 기능 수동 검증</name>
  <files></files>
  <action>
Human verifies the complete easy payment flow on the running application:
1. Start server: `cd /Users/Dev/PMS/hola-pms and ./gradlew :hola-app:bootRun`
2. Navigate to booking engine checkout page
3. Verify email-empty guidance message appears when clicking easy pay tab
4. Verify card list loads after entering email
5. Optionally test KICC card registration flow
  </action>
  <verify>Human confirms all visual and functional checks pass</verify>
  <done>Human approves the easy payment feature completeness</done>
  <what-built>간편결제(빌키) 기능 갭 보완 완료:
1. MockPaymentGateway에 registerTransaction/approveAfterAuth mock 메서드 추가
2. EasyPayApiController/EasyPayCardServiceImpl의 KiccApiClient 의존성을 optional로 변경 (test profile 호환)
3. EasyPayCardServiceImpl 단위 테스트 8개 작성 (등록/삭제/조회/제한)
4. booking.js: 이메일 미입력 시 안내 메시지 추가 (D-01)
5. booking.js: 모바일 pageshow 이벤트 + billkey-result 폴링 추가</what-built>
  <how-to-verify>
1. 서버 실행: `cd /Users/Dev/PMS/hola-pms && ./gradlew :hola-app:bootRun`
2. http://localhost:8080 접속 후 부킹엔진 checkout 화면으로 이동
3. 이메일 입력 없이 "간편결제" 탭 클릭 → "이메일을 먼저 입력하면 등록된 카드를 확인할 수 있습니다." 메시지 확인
4. 이메일 입력 후 간편결제 탭 → 카드 목록 조회 API 호출 확인 (빈 목록 + 카드 추가 버튼)
5. (선택) KICC 테스트 환경에서 카드 등록 → 카드 목록 갱신 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `./gradlew compileJava` passes
- `./gradlew :hola-reservation:test` passes (including EasyPayCardServiceImplTest)
- Human verifies checkout page email guidance and card flow
</verification>

<success_criteria>
- All compilation and tests green
- Human confirms email guidance message displays correctly
- Human confirms easy pay tab flow works as expected
</success_criteria>

<output>
After completion, create `.planning/phases/02-easy-payment/02-03-SUMMARY.md`
</output>
